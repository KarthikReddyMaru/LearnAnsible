- hosts: all
  become: true
  
  tasks:
  - name: Update indexes
    apt:
      update_cache: true
    when: ansible_facts["distribution"] == "Ubuntu"
  - name: Install Apache2 server
    apt:
      name: apache2 
    when: ansible_facts["distribution"] == "Ubuntu"
